# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository ("Better11") initialized with only a `README.md`. As of initial setup, there is no source code, no dependency manifests, no build tooling, and no services.

**Current state:** Empty repo — no lint, test, build, or run commands exist yet.

Once the project scaffolding is added, update this section with:
- How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and the dev server
- Any non-obvious environment requirements or caveats
