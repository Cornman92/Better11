<Page
    x:Class="Better11.App.Views.FileOperationsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Better11.App.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="24,24,24,16" Spacing="8">
            <TextBlock
                Text="File Operations"
                Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock
                Text="Batch file operations: copy, move, delete, and hash verification"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="24,0,24,24" Spacing="24">

                <!-- Source and Destination Selection -->
                <StackPanel Spacing="12">
                    <TextBlock
                        Text="Path Selection"
                        Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <!-- Source Path -->
                    <StackPanel Spacing="4">
                        <TextBlock Text="Source Path"/>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox
                                Grid.Column="0"
                                Text="{x:Bind ViewModel.SourcePath, Mode=TwoWay}"
                                PlaceholderText="Select source folder..."/>

                            <Button
                                Grid.Column="1"
                                Content="Browse"
                                Command="{x:Bind ViewModel.BrowseSourceCommand}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Destination Path -->
                    <StackPanel Spacing="4">
                        <TextBlock Text="Destination Path"/>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox
                                Grid.Column="0"
                                Text="{x:Bind ViewModel.DestinationPath, Mode=TwoWay}"
                                PlaceholderText="Select destination folder..."/>

                            <Button
                                Grid.Column="1"
                                Content="Browse"
                                Command="{x:Bind ViewModel.BrowseDestinationCommand}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Search Options -->
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox
                            Grid.Column="0"
                            Header="Search Pattern"
                            Text="{x:Bind ViewModel.SearchPattern, Mode=TwoWay}"
                            PlaceholderText="*.*"/>

                        <CheckBox
                            Grid.Column="1"
                            Content="Recursive"
                            IsChecked="{x:Bind ViewModel.Recursive, Mode=TwoWay}"
                            VerticalAlignment="Bottom"
                            Margin="0,0,0,4"/>

                        <Button
                            Grid.Column="2"
                            Content="Scan Files"
                            Command="{x:Bind ViewModel.ScanFilesCommand}"
                            Style="{StaticResource AccentButtonStyle}"
                            VerticalAlignment="Bottom">
                            <Button.Icon>
                                <FontIcon Glyph="&#xE721;"/>
                            </Button.Icon>
                        </Button>
                    </Grid>
                </StackPanel>

                <!-- File List -->
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock
                            Text="Files"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            HorizontalAlignment="Left"/>

                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="8"
                            HorizontalAlignment="Right">
                            <Button
                                Content="Select All"
                                Command="{x:Bind ViewModel.SelectAllCommand}"
                                CommandParameter="True"
                                Style="{StaticResource TextBlockButtonStyle}"/>
                            <Button
                                Content="Deselect All"
                                Command="{x:Bind ViewModel.SelectAllCommand}"
                                CommandParameter="False"
                                Style="{StaticResource TextBlockButtonStyle}"/>
                        </StackPanel>
                    </Grid>

                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        MinHeight="200"
                        MaxHeight="300">
                        <ListView
                            ItemsSource="{x:Bind ViewModel.FileList, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:FileOperationItem">
                                    <Grid Padding="8" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox
                                            Grid.Column="0"
                                            IsChecked="{x:Bind Selected, Mode=TwoWay}"/>

                                        <FontIcon
                                            Grid.Column="1"
                                            Glyph="&#xE8A5;"
                                            FontSize="20"/>

                                        <StackPanel Grid.Column="2" Spacing="2">
                                            <TextBlock
                                                Text="{x:Bind Name}"
                                                Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                            <TextBlock
                                                Text="{x:Bind Path}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                TextTrimming="CharacterEllipsis"/>
                                            <TextBlock
                                                Text="{x:Bind Hash, Mode=OneWay}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                FontFamily="Consolas"
                                                TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <TextBlock
                                            Grid.Column="3"
                                            Text="{x:Bind SizeFormatted}"
                                            VerticalAlignment="Center"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>

                <!-- Operations -->
                <StackPanel Spacing="12">
                    <TextBlock
                        Text="Operations"
                        Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button
                            Grid.Column="0"
                            Command="{x:Bind ViewModel.CopyFilesCommand}"
                            HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8C8;"/>
                                <TextBlock Text="Copy"/>
                            </StackPanel>
                        </Button>

                        <Button
                            Grid.Column="1"
                            Command="{x:Bind ViewModel.MoveFilesCommand}"
                            HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DE;"/>
                                <TextBlock Text="Move"/>
                            </StackPanel>
                        </Button>

                        <Button
                            Grid.Column="2"
                            Command="{x:Bind ViewModel.DeleteFilesCommand}"
                            HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74D;"/>
                                <TextBlock Text="Delete"/>
                            </StackPanel>
                        </Button>

                        <Button
                            Grid.Column="3"
                            Command="{x:Bind ViewModel.ComputeHashesCommand}"
                            HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE785;"/>
                                <TextBlock Text="Compute Hashes"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- Progress -->
                    <StackPanel Spacing="4" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
                        <TextBlock
                            Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}"
                            Style="{StaticResource CaptionTextBlockStyle}"/>
                        <ProgressBar
                            Value="{x:Bind ViewModel.ProgressValue, Mode=OneWay}"
                            Maximum="{x:Bind ViewModel.ProgressMaximum, Mode=OneWay}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Operation Log -->
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock
                            Text="Operation Log"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            HorizontalAlignment="Left"/>

                        <Button
                            Content="Clear Log"
                            Command="{x:Bind ViewModel.ClearLogCommand}"
                            HorizontalAlignment="Right"
                            Style="{StaticResource TextBlockButtonStyle}"/>
                    </Grid>

                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        MinHeight="150">
                        <ScrollViewer
                            MaxHeight="200"
                            VerticalScrollBarVisibility="Auto">
                            <TextBlock
                                Text="{x:Bind ViewModel.OperationLog, Mode=OneWay}"
                                FontFamily="Consolas"
                                TextWrapping="Wrap"
                                IsTextSelectionEnabled="True"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Busy Indicator -->
        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <ProgressRing
                    IsActive="True"
                    Width="60"
                    Height="60"/>
                <TextBlock
                    Text="Processing..."
                    Style="{StaticResource SubtitleTextBlockStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
