<Page
    x:Class="Better11.App.Views.AppManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Better11.App.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="24,24,24,16" Spacing="8">
            <TextBlock
                Text="App Manager"
                Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock
                Text="Search, install, and manage applications using winget and Chocolatey"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="24,0,24,24" Spacing="24">

                <!-- Search Section -->
                <StackPanel Spacing="12">
                    <TextBlock
                        Text="Search for Applications"
                        Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <!-- Package Manager Selection -->
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <RadioButton
                            x:Name="WingetRadio"
                            Content="winget"
                            IsChecked="True"
                            IsEnabled="{x:Bind ViewModel.IsWingetAvailable, Mode=OneWay}"
                            Checked="PackageManager_Changed"/>
                        <RadioButton
                            x:Name="ChocoRadio"
                            Content="Chocolatey"
                            IsEnabled="{x:Bind ViewModel.IsChocoAvailable, Mode=OneWay}"
                            Checked="PackageManager_Changed"/>
                    </StackPanel>

                    <!-- Search Box -->
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox
                            Grid.Column="0"
                            Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="Enter application name or keyword..."
                            KeyDown="SearchBox_KeyDown"/>

                        <Button
                            Grid.Column="1"
                            Content="Search"
                            Command="{x:Bind ViewModel.SearchAppsCommand}"
                            Style="{StaticResource AccentButtonStyle}">
                            <Button.Icon>
                                <FontIcon Glyph="&#xE721;"/>
                            </Button.Icon>
                        </Button>
                    </Grid>

                    <!-- Search Results -->
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        MinHeight="200"
                        MaxHeight="300">
                        <ListView
                            ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}"
                            SelectedItem="{x:Bind ViewModel.SelectedPackage, Mode=TwoWay}"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:AppPackage">
                                    <Grid Padding="8" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <FontIcon
                                            Grid.Column="0"
                                            Glyph="&#xE74C;"
                                            FontSize="24"/>

                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock
                                                Text="{x:Bind Name}"
                                                Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                            <TextBlock
                                                Text="{x:Bind Id}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>

                                        <TextBlock
                                            Grid.Column="2"
                                            Text="{x:Bind Version}"
                                            VerticalAlignment="Center"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button
                            Content="Install"
                            Command="{x:Bind ViewModel.InstallPackageCommand}"
                            Style="{StaticResource AccentButtonStyle}">
                            <Button.Icon>
                                <FontIcon Glyph="&#xE896;"/>
                            </Button.Icon>
                        </Button>

                        <Button
                            Content="Uninstall"
                            Command="{x:Bind ViewModel.UninstallPackageCommand}">
                            <Button.Icon>
                                <FontIcon Glyph="&#xE74D;"/>
                            </Button.Icon>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Installed Apps Section -->
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock
                            Text="Installed Applications"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            HorizontalAlignment="Left"/>

                        <Button
                            Content="Refresh"
                            Command="{x:Bind ViewModel.LoadInstalledAppsCommand}"
                            HorizontalAlignment="Right"
                            Style="{StaticResource TextBlockButtonStyle}">
                            <Button.Icon>
                                <FontIcon Glyph="&#xE72C;"/>
                            </Button.Icon>
                        </Button>
                    </Grid>

                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        MinHeight="200"
                        MaxHeight="300">
                        <ListView
                            ItemsSource="{x:Bind ViewModel.InstalledApps, Mode=OneWay}"
                            SelectedItem="{x:Bind ViewModel.SelectedPackage, Mode=TwoWay}"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:AppPackage">
                                    <Grid Padding="8" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <FontIcon
                                            Grid.Column="0"
                                            Glyph="&#xE73A;"
                                            FontSize="24"
                                            Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>

                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock
                                                Text="{x:Bind Name}"
                                                Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                            <TextBlock
                                                Text="{x:Bind Id}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>

                                        <TextBlock
                                            Grid.Column="2"
                                            Text="{x:Bind Version}"
                                            VerticalAlignment="Center"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>

                <!-- Operation Log -->
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock
                            Text="Operation Log"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            HorizontalAlignment="Left"/>

                        <Button
                            Content="Clear Log"
                            Command="{x:Bind ViewModel.ClearLogCommand}"
                            HorizontalAlignment="Right"
                            Style="{StaticResource TextBlockButtonStyle}"/>
                    </Grid>

                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        MinHeight="150">
                        <ScrollViewer
                            MaxHeight="200"
                            VerticalScrollBarVisibility="Auto">
                            <TextBlock
                                Text="{x:Bind ViewModel.OperationLog, Mode=OneWay}"
                                FontFamily="Consolas"
                                TextWrapping="Wrap"
                                IsTextSelectionEnabled="True"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Busy Indicator -->
        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <ProgressRing
                    IsActive="True"
                    Width="60"
                    Height="60"/>
                <TextBlock
                    Text="Processing..."
                    Style="{StaticResource SubtitleTextBlockStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
