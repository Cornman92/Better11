# Better11 - Windows 11 Enhancement Toolkit
# Python Requirements

# Core dependencies
# (Add any existing dependencies here)

# Configuration Management (v0.3.0+)
tomli>=2.0.1; python_version < '3.11'  # TOML parsing for Python < 3.11
pyyaml>=6.0.1                          # YAML configuration support

# Windows Integration (v0.3.0+)
pywin32>=305; sys_platform == 'win32'  # Windows COM APIs and Win32 functions

# Code Signing & Cryptography (v0.3.0+)
cryptography>=41.0.0                   # Signature verification and crypto operations

# Networking & Updates (v0.3.0+)
requests>=2.31.0                       # HTTP requests for updates and downloads
urllib3>=2.0.0                         # HTTP client
certifi>=2023.7.22                     # SSL certificate bundle

# System Monitoring (v0.3.0+)
psutil>=5.9.5                          # Cross-platform system and process utilities

# Version Management (v0.3.0+)
packaging>=23.2                        # Version parsing and comparison

# GUI Enhancements (v0.4.0+)
pillow>=10.1.0                         # Image processing for GUI
# matplotlib>=3.8.0                    # Charts and graphs (future)

# Reporting (v0.5.0+)
# jinja2>=3.1.2                        # Report templates (future)
# weasyprint>=60.0                     # PDF generation (future)

# Development Dependencies
pytest>=7.4.0                          # Testing framework
pytest-cov>=4.1.0                      # Code coverage
pytest-mock>=3.12.0                    # Mocking support

# Code Quality
black>=23.11.0                         # Code formatting
flake8>=6.1.0                          # Linting
mypy>=1.7.0                            # Type checking
isort>=5.12.0                          # Import sorting

# Documentation
sphinx>=7.2.0                          # Documentation generation
sphinx-rtd-theme>=2.0.0                # ReadTheDocs theme

# Packaging
build>=1.0.3                           # Reproducible wheel builds for installer packaging
